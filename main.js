let newString = ""
for (let i = 0; i <= item.length; i++) const arr = [item]
const reducer = (currentValue, currentIndex) => (
  (newString + currentValue) / item.length, currentIndex
)

index.html.querySelector("#answer1")
const answer1 = {
  querySelector: function (reducer) {
    console.log(newString)
  },
}
